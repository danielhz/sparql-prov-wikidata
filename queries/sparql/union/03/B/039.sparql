PREFIX wd: <http://www.wikidata.org/entity/>
PREFIX wdt: <http://www.wikidata.org/prop/direct/>
SELECT *
WHERE {
  {
    SELECT ?x ?y1
    WHERE {
      ?x
        wdt:P27 wd:Q31 ;
        wdt:P734 ?y1 ;
        wdt:P735 ?y2a
    }
  }
  UNION
  {
    SELECT ?x ?y1
    WHERE {
      ?x
        wdt:P27 wd:Q31 ;
        wdt:P734 ?y1 ;
        wdt:P735 ?y2b
    }
  }
}
