PREFIX wd: <http://www.wikidata.org/entity/>
PREFIX wdt: <http://www.wikidata.org/prop/direct/>
SELECT *
WHERE {
  {
    SELECT ?x ?y1
    WHERE {
      ?x
        wdt:P50 wd:Q48269450 ;
        wdt:P1433 ?y1 ;
        wdt:P2860 ?y2a
    }
  }
  UNION
  {
    SELECT ?x ?y1
    WHERE {
      ?x
        wdt:P50 wd:Q48269450 ;
        wdt:P1433 ?y1 ;
        wdt:P2860 ?y2b
    }
  }
}
