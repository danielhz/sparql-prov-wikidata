PREFIX wd: <http://www.wikidata.org/entity/>
PREFIX wdt: <http://www.wikidata.org/prop/direct/>
SELECT *
WHERE {
  {
    SELECT ?x ?y1
    WHERE {
      ?x
        wdt:P527 wd:Q24784524 ;
        wdt:P361 ?y1 ;
        wdt:P682 ?y2a
    }
  }
  UNION
  {
    SELECT ?x ?y1
    WHERE {
      ?x
        wdt:P527 wd:Q24784524 ;
        wdt:P361 ?y1 ;
        wdt:P682 ?y2b
    }
  }
}
