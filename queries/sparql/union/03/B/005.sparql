PREFIX wd: <http://www.wikidata.org/entity/>
PREFIX wdt: <http://www.wikidata.org/prop/direct/>
SELECT *
WHERE {
  {
    SELECT ?x ?y1
    WHERE {
      ?x
        wdt:P421 wd:Q6985 ;
        wdt:P17 ?y1 ;
        wdt:P131 ?y2a
    }
  }
  UNION
  {
    SELECT ?x ?y1
    WHERE {
      ?x
        wdt:P421 wd:Q6985 ;
        wdt:P17 ?y1 ;
        wdt:P131 ?y2b
    }
  }
}
