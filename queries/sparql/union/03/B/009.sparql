PREFIX wd: <http://www.wikidata.org/entity/>
PREFIX wdt: <http://www.wikidata.org/prop/direct/>
SELECT *
WHERE {
  {
    SELECT ?x ?y1
    WHERE {
      ?x
        wdt:P17 wd:Q148 ;
        wdt:P131 ?y1 ;
        wdt:P421 ?y2a
    }
  }
  UNION
  {
    SELECT ?x ?y1
    WHERE {
      ?x
        wdt:P17 wd:Q148 ;
        wdt:P131 ?y1 ;
        wdt:P421 ?y2b
    }
  }
}
