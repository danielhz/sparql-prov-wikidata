PREFIX wd: <http://www.wikidata.org/entity/>
PREFIX wdt: <http://www.wikidata.org/prop/direct/>
SELECT *
WHERE {
  {
    SELECT ?x ?y1
    WHERE {
      ?x
        wdt:P688 wd:Q61542982 ;
        wdt:P684 ?y1 ;
        wdt:P703 ?y2a
    }
  }
  UNION
  {
    SELECT ?x ?y1
    WHERE {
      ?x
        wdt:P688 wd:Q61542982 ;
        wdt:P684 ?y1 ;
        wdt:P703 ?y2b
    }
  }
}
